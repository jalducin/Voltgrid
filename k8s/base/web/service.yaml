apiVersion: v1
kind: Service
metadata:
  name: voltgrid-web
  labels:
    app.kubernetes.io/name: voltgrid-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: voltgrid-web
  ports:
    - name: http
      port: 3000
      targetPort: http
